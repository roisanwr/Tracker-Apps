name: workout_tracker
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # ⚡ UPGRADE: Library Timezone, Supabase, & Intl
  cupertino_icons: ^1.0.6
  supabase_flutter: ^2.6.0
  flutter_timezone: ^3.0.1 
  intl: ^0.19.0 # 👈 Tambahkan baris ini (Format Tanggal & Angka)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/